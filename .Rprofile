tryCatch({ renv::load(); renv::restore(prompt = FALSE); }, 
         error = function(e) { source(c("renv/activate.R","R/Script/functions.R")) })
################################################################################
# import packages
library(multcomp)   # multiple comparison
library(readxl)     # read_excel
library(tidyverse)  # ggplot2(3.3.5),purrr(0.3.4),tibble(3.1.5),dplyr(1.0.7),
                    # tidyr(1.1.4),tringr(1.4.0),readr(2.0.2),forcats(0.5.1)
# library(rJava)    # error
# library(xlsx)     # error
library(openxlsx)   # write.xlsx
library(writexl)
################################################################################
# BioConductor
# library(DEP)
# library(SummarizedExperiment)
# library(org.Hs.eg.db)
# library(org.Mm.eg.db)
# library(GO.db)
# library(PANTHER.db)
# library(biomaRt)
################################################################################






